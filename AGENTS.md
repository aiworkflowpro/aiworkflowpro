# awp-config-github-org — Agent instructions

## 项目
aiworkflowpro 的公开 GitHub profile 仓；远端仓名是 `aiworkflowpro`。

## 目录
- `README.md` 是公开主页正文。
- `assets/` 存放横幅等图像。
- `.github/` 是 GitHub profile 配置目录。
- 本仓没有应用源码。
- 本仓没有包管理器清单。
- 首页包含品牌简介和频道入口。
- 首页包含视频 playbook 表。
- 相对资源路径从 README 所在目录解析。

## 可用命令
- 本仓没有本地 build/test 脚本。
- 文案变更看 `git diff -- README.md`。
- 图片变更核对 `assets/` 中实际文件。
- 外链变更逐项打开目标。

## 边界
- 不要改 GitHub profile 仓远端名。
- 频道链接须对应当前官方账号。
- 网站链接须对应当前站点。
- 视频链接须对应已发布视频。
- 未发布 playbook 不写成已可访问。
- 表格序号和视频系列保持一致。
- 图片改名时同步更新 README。
- 公开页面不写内部路径。
- 公开页面不写凭据。
- 保持现有页面视觉结构。
- 文案只描述当前项目事实。
- 本仓不承担应用部署说明。
- README 是本仓的主要交付物。

## 工作方式
- 先读本仓 README，再查看当前文件和脚本。
- 只改本次任务涉及的文件。
- 不把运行数据、真实环境变量或凭据提交进仓。
- 新增本地引用前，检查目标文件存在。
- 修改公开文案前，核对事实和目标地址。
- 运行与改动相称的检查。
- 提交前运行 `git diff --check`。
- 提交前检查 `git status --short`。
- 只暂存本次改动文件。
- 提交信息写清实际改动，不带 AI 工具署名。
- 本仓已有的无关改动保持原样。
- 未执行的验证步骤在回报中写清。

## 本次改动核对
- 先看 diff，确认没有额外文件。
- 再核对 README 中与改动有关的入口。
- 验证通过后只提交本次改动。

## 变更定位
- 品牌简介与渠道入口在 README 前半部分。
- 视频 playbook 表在 README 后半部分。
- 横幅引用在 README 顶部。
- 新增图片时检查相对路径和尺寸。
- 变更后按公开页面阅读一次完整 README。
